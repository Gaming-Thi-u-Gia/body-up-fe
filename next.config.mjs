/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["res.cloudinary.com", "lh3.googleusercontent.com", 'i.ytimg.com', 'otherdomain.com'],
    },
};

export default nextConfig;
